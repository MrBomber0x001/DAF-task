/* Replace with your SQL commands */
CREATE TABLE "users" (
  "id" SERIAL PRIMARY KEY,
  "firstname" varchar,
  "middlename" varchar,
  "lastname" varchar,
  "phone" int,
  "email" varchar,
  "password" varchar,
  "token" varchar
);